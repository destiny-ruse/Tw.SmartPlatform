image:
  repository: registry.example.com/tw-service
  tag: "0.1.0"
  pullPolicy: IfNotPresent

service:
  port: 8080

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

probes:
  readinessPath: /healthz
  livenessPath: /healthz

secrets:
  existingSecret: ""
