# Shared Package

## When To Load

Load this index when a task creates, changes, or reviews a shared package or common building block under `backend/dotnet/BuildingBlocks/src` or `frontend/packages`, including public capability changes, package boundaries, `package-charter.yaml`, and shared-package usage docs under `docs/shared-packages`.

## Required Formal Standards

- `docs/engineering-standards/03-project-and-code/shared-package-charter.md`

## Execution Requirements

- Read the referenced formal standard files before changing this task area.
- If this task overlaps another task category, load the matching task index from `01-task-router.md`.
