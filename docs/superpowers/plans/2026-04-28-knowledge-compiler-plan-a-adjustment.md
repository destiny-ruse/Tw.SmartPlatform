# Knowledge Compiler 方案 A 全量调整 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐 Knowledge Compiler 方案 A 的跨栈 taxonomy、全量扫描、节点初始化、结构化 diff、契约漂移诊断、图谱种子、目录协议和文档化验证链路。

**Architecture:** 在现有 `tools/knowledge/knowledge.py` 单文件 CLI 和 `tests/knowledge/test_knowledge_tool.py` 单元测试基础上做增量扩展，继续只使用 Python 标准库和仓库内 YAML 子集解析器。`scan`、`diff`、`check-drift --save` 复用当前 taxonomy 路径匹配和诊断输出模型，`init` 复用模板渲染生成正式 graph YAML，生成索引仍由 `generate/check` 统一兜底。

**Tech Stack:** Python standard library, `unittest`, JSON, constrained YAML subset, Markdown, PowerShell-compatible commands.

---

## Standards

本计划按以下工程规范落地，实施时在 PR 描述或验证记录中引用同一组标准：

- `rules.repo-layout#rules` v1.1.0, `docs/standards/rules/repo-layout.md`: 文档放 `docs/`，工具放 `tools/`，测试目录能关联被测源码，生成产物不得手工维护。
- `rules.test-strategy#rules` v1.1.0, `docs/standards/rules/test-strategy.md`: 每个功能和缺陷修复都声明验证策略，单元测试覆盖边界和错误分支。
- `rules.naming-python#rules` v1.1.0, `docs/standards/rules/naming-python.md`: Python 函数、变量使用 `snake_case`，测试函数使用可读的 `test_<scenario>_<expected>`。
- `processes.doc-authoring#rules` v1.0.0, `docs/standards/processes/doc-authoring.md`: 关键结论可追踪，文档记录输入、输出、验证命令和验收证据。

## File Structure

Modify these files:

- `docs/knowledge/taxonomy.yaml`: 增加全栈 path rules、`valid_module_types`、`go` 预留、查询别名和 `knowledge.contract-outdated` 诊断。
- `docs/knowledge/README.md`: 增加新栈接入协议、`scan/init/diff/check-drift --save` 命令说明、`changes/` 与 `proposals/` 写入边界。
- `docs/knowledge/memory.schema.json`: 用 JSON 合法的 `$comment` 说明该文件是文档参考，Python 校验逻辑是唯一权威。
- `tools/knowledge/knowledge.py`: 增加 module_type 校验、`integration.contract` 引用校验、contract-outdated 诊断、filesystem scan、init、diff、drift save 和动态 skill 同步。
- `tests/knowledge/test_knowledge_tool.py`: 增加所有新行为的单元测试和命令级测试。
- `tools/knowledge/templates/module.yaml`: 调整为 `init` 可渲染的 draft 节点模板。
- `tools/knowledge/templates/capability.yaml`: 调整为 `init` 可渲染的 draft 节点模板。

Create these files:

- `tools/knowledge/templates/contract.yaml`: contract 节点模板。
- `tools/knowledge/templates/integration.yaml`: integration 节点模板，包含可选 `contract` 字段。
- `tools/knowledge/templates/decision.yaml`: decision 节点模板。
- `tools/knowledge/rules/README.md`: 诊断规则文档目录入口。
- `tools/knowledge/rules/knowledge-diagnostics.md`: 诊断码、触发条件、schema 字段和处理建议。
- `docs/knowledge/graph/contracts/.gitkeep`: 跟踪 contract graph 目录。
- `docs/knowledge/changes/2026/.gitkeep`: 跟踪 drift save 年度目录。
- `docs/knowledge/proposals/.gitkeep`: 跟踪候选图谱目录。
- `docs/knowledge/graph/modules/frontend.apps.tw-app-owner.yaml`: 业主端 App module 节点。
- `docs/knowledge/graph/modules/frontend.apps.tw-app-staff.yaml`: 员工端 App module 节点。
- `docs/knowledge/graph/modules/frontend.apps.tw-web-client.yaml`: 客户 Web 端 module 节点。
- `docs/knowledge/graph/modules/frontend.apps.tw-web-ops.yaml`: 运营 Web 端 module 节点。
- `docs/knowledge/graph/modules/frontend.apps.tw-web-portal.yaml`: 门户 Web 端 module 节点。

Generated by commands:

- `docs/knowledge/generated/**/*.generated.json`: 由 `python tools/knowledge/knowledge.py generate` 生成。
- `docs/knowledge/changes/2026/*.json`: 由 `python tools/knowledge/knowledge.py check-drift --save` 生成。
- `.claude/skills/*`: 由 `python tools/knowledge/knowledge.py sync-skills --target claude` 生成相对符号链接。

Do not modify unrelated working-tree files. Current unrelated changes include `.gitignore` and `.claude/settings.local.json`; leave them untouched.

## Implementation Tasks

### Task 1: Extend Taxonomy And Module Type Validation

**Files:**
- Modify: `docs/knowledge/taxonomy.yaml`
- Modify: `tools/knowledge/knowledge.py`
- Modify: `tests/knowledge/test_knowledge_tool.py`

- [ ] **Step 1: Write failing taxonomy and module_type tests**

Append these tests to `KnowledgeToolTests` in `tests/knowledge/test_knowledge_tool.py`:

```python
    def test_taxonomy_contains_plan_a_cross_stack_rules(self):
        with isolated_repo() as root:
            write_taxonomy(root)
            taxonomy = knowledge.load_taxonomy()

            patterns = {rule["pattern"] for rule in taxonomy["path_rules"]}
            self.assertIn("backend/java/services/*/**", patterns)
            self.assertIn("backend/java/packages/*/**", patterns)
            self.assertIn("backend/python/services/*/**", patterns)
            self.assertIn("backend/python/packages/*/**", patterns)
            self.assertIn("frontend/apps/*/**", patterns)
            self.assertIn("contracts/openapi/**/*.yaml", patterns)
            self.assertIn("go", taxonomy["valid_stacks"])
            self.assertIn("framework-package", taxonomy["valid_module_types"])
            self.assertIn("frontend-app", taxonomy["valid_module_types"])
            self.assertIn("knowledge.contract-outdated", taxonomy["diagnostics"])

    def test_validation_rejects_invalid_module_type(self):
        with isolated_repo() as root:
            write_taxonomy(root)
            write_file(root, "backend/java/services/user/README.md", "# 用户服务\n")
            write_file(
                root,
                "docs/knowledge/graph/modules/backend.java.services.user.yaml",
                """
                schema_version: 1.0.0
                id: backend.java.services.user
                kind: module
                name: 用户服务
                status: draft
                summary: 承载 Java 用户服务代码。
                owners:
                  - platform
                tags:
                  - backend
                  - java
                module_type: bad-type
                stack: java
                path: backend/java/services/user
                source:
                  declared_in: docs/knowledge/graph/modules/backend.java.services.user.yaml
                  evidence:
                    - backend/java/services/user/README.md
                provenance:
                  created_by: human
                  created_at: 2026-04-28
                  updated_by: human
                  updated_at: 2026-04-28
                """,
            )

            messages = knowledge.collect_validation_messages()

            self.assertIn("knowledge.invalid-module-type", [message.code for message in messages])
            self.assertIn("bad-type", diagnostic_text(messages))

    def test_framework_package_drift_is_warning(self):
        with isolated_repo() as root:
            write_taxonomy(root)
            write_file(root, "backend/java/packages/common/README.md", "# common\n")

            diagnostics = knowledge.detect_drift_from_paths(["backend/java/packages/common/README.md"])

            self.assertEqual("WARN", diagnostics[0].level)
            self.assertEqual("knowledge.missing-capability", diagnostics[0].code)
            self.assertEqual("backend/java/packages/common", diagnostics[0].location)
```

Replace `write_taxonomy()` in the same file with the exact taxonomy content from Step 3 below so tests use the new expected defaults.

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_taxonomy_contains_plan_a_cross_stack_rules -v
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_validation_rejects_invalid_module_type -v
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_framework_package_drift_is_warning -v
```

Expected:

- The taxonomy test fails until `write_taxonomy()` and `docs/knowledge/taxonomy.yaml` include the new fields.
- The invalid module type test fails until `collect_validation_messages()` checks `valid_module_types`.
- The framework package test fails until `path_rule_diagnostic()` treats `framework-package` like `building-block`.

- [ ] **Step 3: Update `docs/knowledge/taxonomy.yaml`**

Replace `docs/knowledge/taxonomy.yaml` with:

```yaml
schema_version: 1.0.0
valid_kinds:
  - capability
  - module
  - contract
  - integration
  - decision
valid_statuses:
  - active
  - draft
  - deprecated
  - superseded
valid_stacks:
  - dotnet
  - java
  - python
  - vue-ts
  - uniapp
  - go
valid_domains:
  - security
  - integration
  - platform
  - frontend
  - contracts
  - operations
valid_module_types:
  - microservice
  - building-block
  - framework-package
  - frontend-app
  - frontend-package
path_rules:
  - pattern: backend/dotnet/Services/*/**
    kind: module
    module_type: microservice
    stack: dotnet
  - pattern: backend/dotnet/BuildingBlocks/src/*/**
    kind: module
    module_type: building-block
    stack: dotnet
  - pattern: backend/java/services/*/**
    kind: module
    module_type: microservice
    stack: java
  - pattern: backend/java/packages/*/**
    kind: module
    module_type: framework-package
    stack: java
  - pattern: backend/python/services/*/**
    kind: module
    module_type: microservice
    stack: python
  - pattern: backend/python/packages/*/**
    kind: module
    module_type: framework-package
    stack: python
  - pattern: backend/go/services/*/**
    kind: module
    module_type: microservice
    stack: go
  - pattern: backend/go/packages/*/**
    kind: module
    module_type: framework-package
    stack: go
  - pattern: frontend/apps/*/**
    kind: module
    module_type: frontend-app
    stack: vue-ts
  - pattern: frontend/packages/*/**
    kind: module
    module_type: frontend-package
    stack: vue-ts
  - pattern: contracts/protos/**/*.proto
    kind: contract
    contract_type: grpc
  - pattern: contracts/openapi/**/*.yaml
    kind: contract
    contract_type: openapi
diagnostics:
  knowledge.missing-module:
    severity: error
    message_zh: 新增服务或模块目录尚未声明 module 图谱节点。
    suggestion_zh: 新增对应 module 图谱节点，或在 taxonomy.yaml 中声明忽略规则。
  knowledge.missing-capability:
    severity: warning
    message_zh: 新增公共构件目录可能暴露可复用能力，但尚未声明 capability 图谱节点。
    suggestion_zh: 为该公共构件声明 capability，或说明它不对外提供复用能力。
  knowledge.contract-drift:
    severity: error
    message_zh: 公共契约发生变更，但对应 contract 图谱节点未更新。
    suggestion_zh: 更新 contract 节点的版本、兼容性说明或变更证据。
  knowledge.contract-outdated:
    severity: warning
    message_zh: 契约文件发生变更，对应 contract 图谱节点可能未同步更新。
    suggestion_zh: 检查 contract 节点版本、兼容性说明和变更证据是否已更新。
query_aliases:
  当前用户:
    - auth
    - authentication
    - user
  授权:
    - auth
    - authorization
    - permission
  远程调用:
    - remote-call
    - service-integration
  Spring Boot:
    - java
    - spring
    - packages
  前端:
    - vue
    - frontend
    - apps
```

- [ ] **Step 4: Add module_type validation**

In `collect_validation_messages()`, after `valid_statuses` is assigned, add:

```python
    valid_module_types = set(taxonomy.get("valid_module_types") or [])
```

In the per-node loop, after the status validation block, add:

```python
        module_type = data.get("module_type")
        if kind == "module" and module_type and valid_module_types and module_type not in valid_module_types:
            messages.append(
                error(
                    "knowledge.invalid-module-type",
                    location,
                    f"module_type 不在 taxonomy.yaml 允许范围内: {module_type}",
                    "请使用 taxonomy.yaml valid_module_types 中声明的模块类型。",
                )
            )
```

- [ ] **Step 5: Treat framework packages as warning diagnostics**

Replace the `if rule.get("module_type") == "building-block":` block in `path_rule_diagnostic()` with:

```python
    if rule.get("module_type") in {"building-block", "framework-package"}:
        return taxonomy_diagnostic(
            taxonomy,
            "knowledge.missing-capability",
            "WARN",
            location,
            "新增公共构件目录可能暴露可复用能力，但尚未声明 capability 图谱节点。",
            "新增对应 module 图谱节点并关联 capability，或说明该公共构件不对外提供复用能力。",
        )
```

- [ ] **Step 6: Run tests and commit**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool -v
python tools/knowledge/knowledge.py check
```

Expected: all tests pass and `check` exits `0`.

Commit:

```powershell
git add docs/knowledge/taxonomy.yaml tools/knowledge/knowledge.py tests/knowledge/test_knowledge_tool.py
git commit -m "feat: extend knowledge taxonomy coverage"
```

### Task 2: Fix Contract Drift And Integration Contract References

**Files:**
- Modify: `tools/knowledge/knowledge.py`
- Modify: `tests/knowledge/test_knowledge_tool.py`

- [ ] **Step 1: Write failing tests**

Add this helper near the other top-level helper functions in `tests/knowledge/test_knowledge_tool.py`:

```python
def write_contract(root: Path) -> None:
    write_file(root, "contracts/protos/authentication/v1/authentication.proto", 'syntax = "proto3";\n')
    write_file(
        root,
        "docs/knowledge/graph/contracts/contracts.grpc.authentication-v1.yaml",
        """
        schema_version: 1.0.0
        id: contracts.grpc.authentication-v1
        kind: contract
        name: 认证 gRPC 契约
        status: draft
        summary: 描述认证服务对外暴露的 gRPC 契约。
        owners:
          - platform
        tags:
          - grpc
          - contracts
        contract_type: grpc
        path: contracts/protos/authentication/v1/authentication.proto
        source:
          declared_in: docs/knowledge/graph/contracts/contracts.grpc.authentication-v1.yaml
          evidence:
            - contracts/protos/authentication/v1/authentication.proto
        provenance:
          created_by: human
          created_at: 2026-04-28
          updated_by: human
          updated_at: 2026-04-28
        """,
    )
```

Append these tests to `KnowledgeToolTests`:

```python
    def test_validation_checks_integration_contract_reference(self):
        with isolated_repo() as root:
            write_taxonomy(root)
            write_file(root, "backend/dotnet/Services/Notice/README.md", "# 通知服务\n")
            write_file(root, "backend/dotnet/Services/Authentication/README.md", "# 认证服务\n")
            write_notice_module(root)
            write_module(root)
            write_remote_service_capability(root)
            write_file(
                root,
                "docs/knowledge/graph/integrations/backend.integration.notice-authentication.yaml",
                """
                schema_version: 1.0.0
                id: backend.integration.notice-authentication
                kind: integration
                name: 通知服务到认证服务集成
                status: draft
                summary: 记录通知服务调用认证服务的集成边界。
                owners:
                  - platform
                tags:
                  - backend
                  - service-integration
                caller: backend.dotnet.services.notice
                callee: backend.dotnet.services.authentication
                contract: contracts.grpc.missing
                tooling:
                  required_capabilities:
                    - backend.capability.remote-service
                source:
                  declared_in: docs/knowledge/graph/integrations/backend.integration.notice-authentication.yaml
                  evidence:
                    - backend/dotnet/Services/Notice/README.md
                provenance:
                  created_by: human
                  created_at: 2026-04-28
                  updated_by: human
                  updated_at: 2026-04-28
                """,
            )

            messages = knowledge.collect_validation_messages()

            self.assertIn("knowledge.dangling-reference", [message.code for message in messages])
            self.assertIn("contract 引用未声明的 contract 图谱节点", diagnostic_text(messages))

    def test_detect_drift_warns_when_existing_contract_file_changes(self):
        with isolated_repo() as root:
            write_taxonomy(root)
            write_contract(root)

            diagnostics = knowledge.detect_drift_from_paths(
                ["contracts/protos/authentication/v1/authentication.proto"]
            )

            self.assertEqual(1, len(diagnostics), diagnostic_text(diagnostics))
            self.assertEqual("WARN", diagnostics[0].level)
            self.assertEqual("knowledge.contract-outdated", diagnostics[0].code)
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_validation_checks_integration_contract_reference -v
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_detect_drift_warns_when_existing_contract_file_changes -v
```

Expected: the first test fails because `integration.contract` is not checked; the second fails because existing contract changes are skipped.

- [ ] **Step 3: Validate integration.contract**

In `validate_graph_references()`, after `capability_ids` is assigned, add:

```python
    contract_ids = ids_by_kind.get("contract", set())
```

Inside the `if kind == "integration":` block, after `callee` validation, add:

```python
            require_one(node, "contract", "contract", contract_ids, data.get("contract"))
```

- [ ] **Step 4: Emit contract-outdated for existing contract paths**

In `detect_drift_from_paths()`, replace:

```python
            if rule.get("kind") == "contract":
                if changed_path in contract_paths:
                    continue
```

with:

```python
            if rule.get("kind") == "contract":
                if changed_path in contract_paths:
                    add_once(
                        taxonomy_diagnostic(
                            taxonomy,
                            "knowledge.contract-outdated",
                            "WARN",
                            changed_path,
                            "契约文件发生变更，对应 contract 图谱节点可能未同步更新。",
                            "检查 contract 节点版本、兼容性说明和变更证据是否已更新。",
                        )
                    )
                    break
```

- [ ] **Step 5: Run tests and commit**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool -v
python tools/knowledge/knowledge.py check
```

Expected: all tests pass and `check` exits `0`.

Commit:

```powershell
git add tools/knowledge/knowledge.py tests/knowledge/test_knowledge_tool.py
git commit -m "fix: detect contract graph drift"
```

### Task 3: Add Filesystem Scan Command

**Files:**
- Modify: `tools/knowledge/knowledge.py`
- Modify: `tests/knowledge/test_knowledge_tool.py`

- [ ] **Step 1: Write failing scan tests**

Append:

```python
    def test_filesystem_scan_collects_taxonomy_roots_without_git(self):
        with isolated_repo() as root:
            write_taxonomy(root)
            write_file(root, "frontend/apps/tw.web.client/README.md", "# 客户 Web 端\n")
            write_file(root, "backend/java/services/user/README.md", "# 用户服务\n")
            write_file(root, ".git/ignored", "ignored\n")

            paths = knowledge.filesystem_scan_paths()

            self.assertIn("frontend/apps/tw.web.client/README.md", paths)
            self.assertIn("backend/java/services/user/README.md", paths)
            self.assertNotIn(".git/ignored", paths)

    def test_command_scan_uses_drift_detection_without_git_refs(self):
        original_filesystem_scan_paths = knowledge.filesystem_scan_paths
        original_detect_drift_from_paths = knowledge.detect_drift_from_paths
        knowledge.filesystem_scan_paths = lambda: ["frontend/apps/tw.web.client/README.md"]
        knowledge.detect_drift_from_paths = lambda paths: [
            knowledge.error("knowledge.missing-module", paths[0], "missing")
        ]
        stdout = io.StringIO()
        try:
            with contextlib.redirect_stdout(stdout):
                exit_code = knowledge.command_scan(argparse.Namespace())
        finally:
            knowledge.filesystem_scan_paths = original_filesystem_scan_paths
            knowledge.detect_drift_from_paths = original_detect_drift_from_paths

        self.assertEqual(1, exit_code)
        self.assertIn("knowledge.missing-module", stdout.getvalue())
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_filesystem_scan_collects_taxonomy_roots_without_git -v
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_command_scan_uses_drift_detection_without_git_refs -v
```

Expected: fails because `filesystem_scan_paths` and `command_scan` do not exist.

- [ ] **Step 3: Implement filesystem scan helpers**

Add near the drift helpers in `tools/knowledge/knowledge.py`:

```python
IGNORED_SCAN_DIRS = {
    ".git",
    ".claude",
    ".codex",
    ".idea",
    ".vs",
    "bin",
    "obj",
    "node_modules",
    "dist",
    "build",
    "__pycache__",
}


def taxonomy_scan_roots(taxonomy: dict[str, Any]) -> list[str]:
    path_rules = taxonomy.get("path_rules")
    if not isinstance(path_rules, list):
        return []
    roots: set[str] = set()
    for rule in path_rules:
        if not isinstance(rule, dict):
            continue
        pattern = str(rule.get("pattern") or "").strip()
        if not pattern:
            continue
        root = normalized_repo_path(pattern).split("/", 1)[0]
        if root and "*" not in root:
            roots.add(root)
    return sorted(roots)


def filesystem_scan_paths() -> list[str]:
    try:
        taxonomy = load_taxonomy()
    except YamlSubsetError as exc:
        raise RuntimeError(str(taxonomy_parse_diagnostic(exc).message_zh)) from exc

    paths: list[str] = []
    for root_name in taxonomy_scan_roots(taxonomy):
        root = REPO_ROOT / root_name
        if not root.exists():
            continue
        for path in root.rglob("*"):
            if any(part in IGNORED_SCAN_DIRS for part in path.relative_to(REPO_ROOT).parts):
                continue
            if path.is_file():
                paths.append(rel_path(path))
    return sorted(paths)
```

- [ ] **Step 4: Add scan command**

Add:

```python
def command_scan(_args: argparse.Namespace) -> int:
    try:
        paths = filesystem_scan_paths()
    except RuntimeError as exc:
        print("错误 [knowledge.scan]")
        print(f"说明: {exc}")
        return 1

    messages = detect_drift_from_paths(paths)
    emit(messages)
    if has_errors(messages):
        return 1
    if messages:
        print("OK knowledge scan passed with warnings")
    else:
        print("OK knowledge scan passed")
    return 0
```

In `build_parser()`, before `check-drift`, add:

```python
    scan_parser = subparsers.add_parser("scan", help="scan filesystem paths for knowledge graph drift")
    scan_parser.set_defaults(func=command_scan)
```

- [ ] **Step 5: Run tests and commit**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool -v
python tools/knowledge/knowledge.py scan
```

Expected:

- Tests pass.
- `scan` exits non-zero until frontend app seeds are added in Task 6, and its diagnostics include `frontend/apps/tw.app.owner` or another missing frontend app module. This is expected at this point.

Commit:

```powershell
git add tools/knowledge/knowledge.py tests/knowledge/test_knowledge_tool.py
git commit -m "feat: scan filesystem knowledge drift"
```

### Task 4: Add Init Command And New Node Templates

**Files:**
- Modify: `tools/knowledge/knowledge.py`
- Modify: `tests/knowledge/test_knowledge_tool.py`
- Modify: `tools/knowledge/templates/module.yaml`
- Modify: `tools/knowledge/templates/capability.yaml`
- Create: `tools/knowledge/templates/contract.yaml`
- Create: `tools/knowledge/templates/integration.yaml`
- Create: `tools/knowledge/templates/decision.yaml`

- [ ] **Step 1: Write failing init tests**

Append:

```python
    def test_init_writes_module_from_template_without_overwrite(self):
        with isolated_repo() as root:
            write_taxonomy(root)
            exit_code = knowledge.command_init(
                argparse.Namespace(kind="module", node_id="backend.java.services.user")
            )

            target = root / "docs/knowledge/graph/modules/backend.java.services.user.yaml"
            self.assertEqual(0, exit_code)
            self.assertTrue(target.exists())
            content = target.read_text(encoding="utf-8")
            self.assertIn("id: backend.java.services.user", content)
            self.assertIn("path: backend/java/services/user", content)

            second_exit = knowledge.command_init(
                argparse.Namespace(kind="module", node_id="backend.java.services.user")
            )
            self.assertEqual(1, second_exit)

    def test_init_supports_contract_integration_and_decision_kinds(self):
        with isolated_repo() as root:
            write_taxonomy(root)

            for kind, node_id, directory in [
                ("contract", "contracts.openapi.authentication", "contracts"),
                ("integration", "backend.integration.notice-authentication", "integrations"),
                ("decision", "backend.decision.authentication-ownership", "decisions"),
            ]:
                exit_code = knowledge.command_init(argparse.Namespace(kind=kind, node_id=node_id))
                target = root / "docs" / "knowledge" / "graph" / directory / f"{node_id}.yaml"
                self.assertEqual(0, exit_code)
                self.assertTrue(target.exists())
                self.assertIn(f"kind: {kind}", target.read_text(encoding="utf-8"))
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_init_writes_module_from_template_without_overwrite -v
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_init_supports_contract_integration_and_decision_kinds -v
```

Expected: fails because `command_init` does not exist.

- [ ] **Step 3: Implement init helpers**

Add:

```python
KIND_DIRECTORIES = {
    "capability": "capabilities",
    "module": "modules",
    "contract": "contracts",
    "integration": "integrations",
    "decision": "decisions",
}


def graph_path_for_kind_id(kind: str, node_id: str) -> Path:
    directory = KIND_DIRECTORIES.get(kind)
    if not directory:
        raise RuntimeError(f"unsupported graph kind: {kind}")
    return GRAPH_DIR / directory / f"{node_id}.yaml"


def display_name_from_id(node_id: str) -> str:
    return node_id.split(".")[-1].replace("-", " ").title()


def default_module_path(node_id: str) -> str:
    parts = node_id.split(".")
    if len(parts) >= 4 and parts[0] == "backend" and parts[2] in {"services", "packages"}:
        return "/".join(parts[:3] + [parts[3]])
    if len(parts) >= 3 and parts[0] == "frontend" and parts[1] == "apps":
        return f"frontend/apps/{parts[2].replace('-', '.')}"
    if len(parts) >= 3 and parts[0] == "frontend" and parts[1] == "packages":
        return f"frontend/packages/{parts[2]}"
    return ""


def default_contract_path(node_id: str) -> str:
    parts = node_id.split(".")
    if len(parts) >= 3 and parts[0] == "contracts" and parts[1] == "openapi":
        return f"contracts/openapi/{parts[2]}.yaml"
    if len(parts) >= 3 and parts[0] == "contracts" and parts[1] == "grpc":
        return f"contracts/protos/{parts[2]}.proto"
    return ""


def init_template_values(kind: str, node_id: str) -> dict[str, str]:
    target = graph_path_for_kind_id(kind, node_id)
    today = datetime.now(UTC).date().isoformat()
    module_path = default_module_path(node_id) if kind == "module" else ""
    contract_path = default_contract_path(node_id) if kind == "contract" else ""
    return {
        "id": node_id,
        "kind": kind,
        "name": display_name_from_id(node_id),
        "summary": f"{display_name_from_id(node_id)} 的知识图谱节点。",
        "declared_in": rel_path(target),
        "today": today,
        "path": module_path or contract_path,
        "evidence": module_path or contract_path or rel_path(target),
        "module_type": "microservice" if ".services." in node_id else "framework-package",
        "stack": node_id.split(".")[1] if node_id.startswith("backend.") else "vue-ts",
        "contract_type": "openapi" if node_id.startswith("contracts.openapi.") else "grpc",
    }


def render_template(template: str, values: dict[str, str]) -> str:
    rendered = template
    for key, value in values.items():
        rendered = rendered.replace(f"{{{{{key}}}}}", value)
    return rendered
```

- [ ] **Step 4: Add command_init and parser wiring**

Add:

```python
def command_init(args: argparse.Namespace) -> int:
    kind = args.kind
    node_id = args.node_id
    if kind not in KIND_DIRECTORIES:
        print("错误 [knowledge.unsupported-kind]")
        print(f"说明: 不支持的图谱节点类型: {kind}")
        return 1
    if not ID_PATTERN.match(node_id):
        print("错误 [knowledge.id-format]")
        print(f"说明: id 格式无效: {node_id}")
        return 1

    template_path = REPO_ROOT / "tools" / "knowledge" / "templates" / f"{kind}.yaml"
    target_path = graph_path_for_kind_id(kind, node_id)
    if target_path.exists():
        print("错误 [knowledge.init-exists]")
        print(f"说明: 目标图谱节点已存在: {rel_path(target_path)}")
        return 1
    if not template_path.exists():
        print("错误 [knowledge.template-missing]")
        print(f"说明: 模板不存在: {rel_path(template_path)}")
        return 1

    content = render_template(read_text(template_path), init_template_values(kind, node_id))
    write_text(target_path, content)
    print(f"Created {rel_path(target_path)}")
    return 0
```

In `build_parser()`, add:

```python
    init_parser = subparsers.add_parser("init", help="initialize a knowledge graph node from template")
    init_parser.add_argument("--kind", required=True, choices=sorted(KIND_DIRECTORIES))
    init_parser.add_argument("--id", dest="node_id", required=True)
    init_parser.set_defaults(func=command_init)
```

- [ ] **Step 5: Update templates**

Replace `tools/knowledge/templates/module.yaml` with:

```yaml
schema_version: 1.0.0
id: {{id}}
kind: module
name: {{name}}
status: draft
summary: {{summary}}
owners: [platform]
tags: [{{stack}}]
module_type: {{module_type}}
stack: {{stack}}
path: {{path}}
source:
  declared_in: {{declared_in}}
  evidence:
    - {{evidence}}
provenance:
  created_by: human
  created_at: {{today}}
  updated_by: human
  updated_at: {{today}}
```

Replace `tools/knowledge/templates/capability.yaml` with:

```yaml
schema_version: 1.0.0
id: {{id}}
kind: capability
name: {{name}}
status: draft
summary: {{summary}}
owners: [platform]
tags: [platform]
domain: platform
source:
  declared_in: {{declared_in}}
  evidence:
    - {{evidence}}
provenance:
  created_by: human
  created_at: {{today}}
  updated_by: human
  updated_at: {{today}}
```

Create `tools/knowledge/templates/contract.yaml`:

```yaml
schema_version: 1.0.0
id: {{id}}
kind: contract
name: {{name}}
status: draft
summary: {{summary}}
owners: [platform]
tags: [contracts]
contract_type: {{contract_type}}
path: {{path}}
source:
  declared_in: {{declared_in}}
  evidence:
    - {{evidence}}
provenance:
  created_by: human
  created_at: {{today}}
  updated_by: human
  updated_at: {{today}}
```

Create `tools/knowledge/templates/integration.yaml`:

```yaml
schema_version: 1.0.0
id: {{id}}
kind: integration
name: {{name}}
status: draft
summary: {{summary}}
owners: [platform]
tags: [service-integration]
caller:
callee:
contract:
source:
  declared_in: {{declared_in}}
  evidence:
    - {{evidence}}
provenance:
  created_by: human
  created_at: {{today}}
  updated_by: human
  updated_at: {{today}}
```

Create `tools/knowledge/templates/decision.yaml`:

```yaml
schema_version: 1.0.0
id: {{id}}
kind: decision
name: {{name}}
status: draft
summary: {{summary}}
owners: [platform]
tags: [decision]
decision: {{summary}}
source:
  declared_in: {{declared_in}}
  evidence:
    - {{evidence}}
provenance:
  created_by: human
  created_at: {{today}}
  updated_by: human
  updated_at: {{today}}
```

- [ ] **Step 6: Run tests and commit**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool -v
python tools/knowledge/knowledge.py init --kind module --id backend.java.services.user
python tools/knowledge/knowledge.py check
```

Expected:

- Unit tests pass.
- The manual `init` creates `docs/knowledge/graph/modules/backend.java.services.user.yaml`.
- `check` exits `0` or exits `0` with warnings for missing evidence.

Clean up the manual `backend.java.services.user.yaml` file before committing unless the team explicitly wants that seed node in the graph:

```powershell
Remove-Item -LiteralPath docs\knowledge\graph\modules\backend.java.services.user.yaml
```

Commit:

```powershell
git add tools/knowledge/knowledge.py tests/knowledge/test_knowledge_tool.py tools/knowledge/templates
git commit -m "feat: initialize knowledge graph nodes"
```

### Task 5: Add Diff Command And Drift Save Mechanism

**Files:**
- Modify: `tools/knowledge/knowledge.py`
- Modify: `tests/knowledge/test_knowledge_tool.py`
- Create: `docs/knowledge/changes/2026/.gitkeep`

- [ ] **Step 1: Write failing tests**

Append:

```python
    def test_diff_groups_changed_paths_by_taxonomy_kind(self):
        with isolated_repo() as root:
            write_taxonomy(root)
            groups = knowledge.diff_groups_from_paths(
                [
                    ("A", "backend/java/services/user/README.md"),
                    ("M", "contracts/openapi/authentication.yaml"),
                    ("M", "docs/knowledge/README.md"),
                ]
            )

            self.assertIn("module [java microservice]", groups)
            self.assertIn("contract [openapi]", groups)
            self.assertIn("其他文件（不在 taxonomy 规则内）", groups)
            self.assertEqual(["新增: backend/java/services/user/README.md"], groups["module [java microservice]"])
            self.assertEqual(["修改: contracts/openapi/authentication.yaml"], groups["contract [openapi]"])

    def test_save_drift_messages_writes_yearly_json(self):
        with isolated_repo() as root:
            message = knowledge.warn(
                "knowledge.contract-outdated",
                "contracts/protos/authentication/v1/authentication.proto",
                "契约文件发生变更",
            )

            path = knowledge.save_drift_messages([message], "main", "HEAD", today="2026-04-28")

            self.assertEqual(
                root / "docs/knowledge/changes/2026/2026-04-28-main-HEAD.json",
                path,
            )
            payload = json.loads(path.read_text(encoding="utf-8"))
            self.assertEqual("main", payload["from_ref"])
            self.assertEqual("HEAD", payload["to_ref"])
            self.assertEqual("knowledge.contract-outdated", payload["diagnostics"][0]["code"])
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_diff_groups_changed_paths_by_taxonomy_kind -v
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_save_drift_messages_writes_yearly_json -v
```

Expected: fails because `diff_groups_from_paths` and `save_drift_messages` do not exist.

- [ ] **Step 3: Implement diff helpers and save helper**

Add:

```python
STATUS_LABELS = {
    "A": "新增",
    "M": "修改",
    "R": "重命名",
    "C": "复制",
}


def changed_entries_from_name_status(output: str) -> list[tuple[str, str]]:
    entries: list[tuple[str, str]] = []
    for raw_line in output.splitlines():
        if not raw_line.strip():
            continue
        parts = raw_line.split("\t")
        status = parts[0]
        if status.startswith("D"):
            continue
        if status.startswith(("R", "C")) and len(parts) >= 3:
            entries.append((status[0], normalized_repo_path(parts[2])))
            continue
        if len(parts) >= 2:
            entries.append((status[0], normalized_repo_path(parts[1])))
    return entries


def diff_label_for_rule(rule: dict[str, Any]) -> str:
    kind = str(rule.get("kind") or "unknown")
    if kind == "module":
        return f"module [{rule.get('stack')} {rule.get('module_type')}]"
    if kind == "contract":
        return f"contract [{rule.get('contract_type')}]"
    return kind


def diff_groups_from_paths(entries: list[tuple[str, str]]) -> dict[str, list[str]]:
    taxonomy = load_taxonomy()
    rules = taxonomy.get("path_rules") if isinstance(taxonomy.get("path_rules"), list) else []
    groups: dict[str, list[str]] = {}
    other_label = "其他文件（不在 taxonomy 规则内）"
    for status, path in entries:
        label = other_label
        for rule in rules:
            if isinstance(rule, dict) and path_matches_rule(str(rule.get("pattern") or ""), path):
                label = diff_label_for_rule(rule)
                break
        verb = STATUS_LABELS.get(status[0], "修改")
        groups.setdefault(label, []).append(f"{verb}: {path}")
    return {key: sorted(value) for key, value in sorted(groups.items())}


def safe_ref_name(value: str) -> str:
    return re.sub(r"[^A-Za-z0-9._-]+", "-", value).strip("-") or "ref"


def save_drift_messages(
    messages: list[Diagnostic],
    base: str,
    head: str,
    today: str | None = None,
) -> Path:
    current_day = today or datetime.now(UTC).date().isoformat()
    year = current_day.split("-", 1)[0]
    target = KNOWLEDGE_DIR / "changes" / year / f"{current_day}-{safe_ref_name(base)}-{safe_ref_name(head)}.json"
    payload = {
        **generator_metadata(utc_now()),
        "from_ref": base,
        "to_ref": head,
        "diagnostics": [message.to_json() for message in messages],
    }
    write_text(target, json_text(payload))
    return target
```

- [ ] **Step 4: Add command_diff and check-drift --save**

Add:

```python
def command_diff(args: argparse.Namespace) -> int:
    try:
        result = subprocess.run(
            ["git", "diff", "--name-status", args.base, args.head],
            cwd=REPO_ROOT,
            capture_output=True,
            check=False,
            text=True,
        )
        if result.returncode != 0:
            raise RuntimeError(result.stderr.strip() or "git diff failed")
    except RuntimeError as exc:
        print("错误 [knowledge.git-diff]")
        print(f"说明: {exc}")
        return 1

    print(f"变更概览（{args.base} -> {args.head}）")
    groups = diff_groups_from_paths(changed_entries_from_name_status(result.stdout))
    if not groups:
        print("无变更。")
        return 0
    for label, items in groups.items():
        print()
        print(label)
        for item in items:
            print(f"  {item}")
    return 0
```

In `command_check_drift()`, after `messages = detect_drift_from_paths(changed_paths)`, add:

```python
    if getattr(args, "save", False):
        saved_path = save_drift_messages(messages, args.base, args.head)
        print(f"Saved {rel_path(saved_path)}")
```

In `build_parser()`, add `--save` to check-drift and add the diff parser:

```python
    drift_parser.add_argument("--save", action="store_true", help="save diagnostics under docs/knowledge/changes")

    diff_parser = subparsers.add_parser("diff", help="summarize changed paths by knowledge taxonomy")
    diff_parser.add_argument("--from", dest="base", required=True, help="base ref for git diff")
    diff_parser.add_argument("--to", dest="head", required=True, help="head ref for git diff")
    diff_parser.set_defaults(func=command_diff)
```

- [ ] **Step 5: Create changes directory marker**

Create `docs/knowledge/changes/2026/.gitkeep` as an empty UTF-8 file.

- [ ] **Step 6: Run tests and commit**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool -v
python tools/knowledge/knowledge.py diff --from HEAD~1 --to HEAD
python tools/knowledge/knowledge.py check-drift --from HEAD~1 --to HEAD --save
```

Expected:

- Tests pass.
- `diff` prints grouped Chinese output.
- `check-drift --save` writes `docs/knowledge/changes/2026/<date>-HEAD-1-HEAD.json` or the sanitized equivalent for the given refs.

Commit:

```powershell
git add tools/knowledge/knowledge.py tests/knowledge/test_knowledge_tool.py docs/knowledge/changes/2026/.gitkeep docs/knowledge/changes/2026/*.json
git commit -m "feat: summarize and save knowledge drift"
```

### Task 6: Add Graph Directories And Frontend App Seeds

**Files:**
- Create: `docs/knowledge/graph/contracts/.gitkeep`
- Create: `docs/knowledge/proposals/.gitkeep`
- Create: `docs/knowledge/graph/modules/frontend.apps.tw-app-owner.yaml`
- Create: `docs/knowledge/graph/modules/frontend.apps.tw-app-staff.yaml`
- Create: `docs/knowledge/graph/modules/frontend.apps.tw-web-client.yaml`
- Create: `docs/knowledge/graph/modules/frontend.apps.tw-web-ops.yaml`
- Create: `docs/knowledge/graph/modules/frontend.apps.tw-web-portal.yaml`
- Modify: `tests/knowledge/test_knowledge_tool.py`

- [ ] **Step 1: Write failing seed coverage tests**

Append:

```python
    def test_frontend_app_seed_nodes_are_valid_modules(self):
        expected = {
            "frontend.apps.tw-app-owner": "frontend/apps/tw.app.owner",
            "frontend.apps.tw-app-staff": "frontend/apps/tw.app.staff",
            "frontend.apps.tw-web-client": "frontend/apps/tw.web.client",
            "frontend.apps.tw-web-ops": "frontend/apps/tw.web.ops",
            "frontend.apps.tw-web-portal": "frontend/apps/tw.web.portal",
        }
        nodes, messages = knowledge.load_graph_nodes()
        self.assertEqual([], [message for message in messages if message.level == "ERROR"], diagnostic_text(messages))
        modules = {node.data.get("id"): node.data for node in nodes if node.data.get("kind") == "module"}

        for node_id, path in expected.items():
            self.assertIn(node_id, modules)
            self.assertEqual("frontend-app", modules[node_id]["module_type"])
            self.assertEqual("vue-ts", modules[node_id]["stack"])
            self.assertEqual(path, modules[node_id]["path"])
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_frontend_app_seed_nodes_are_valid_modules -v
```

Expected: fails because frontend app module nodes do not exist.

- [ ] **Step 3: Create tracked directories**

Create empty UTF-8 files:

```text
docs/knowledge/graph/contracts/.gitkeep
docs/knowledge/proposals/.gitkeep
```

Confirm current proto count:

```powershell
Get-ChildItem contracts\protos -Recurse -Filter *.proto
```

Expected: no `.proto` files in the current repository snapshot, so this task creates no contract graph node yet.

- [ ] **Step 4: Create frontend app module nodes**

Create `docs/knowledge/graph/modules/frontend.apps.tw-app-owner.yaml`:

```yaml
schema_version: 1.0.0
id: frontend.apps.tw-app-owner
kind: module
name: 业主端 App
status: draft
summary: 承载业主端移动应用的前端代码与交互入口。
owners:
  - frontend
tags:
  - frontend
  - vue-ts
  - uniapp
module_type: frontend-app
stack: vue-ts
path: frontend/apps/tw.app.owner
source:
  declared_in: docs/knowledge/graph/modules/frontend.apps.tw-app-owner.yaml
  evidence:
    - frontend/apps/tw.app.owner/README.md
provenance:
  created_by: human
  created_at: 2026-04-28
  updated_by: human
  updated_at: 2026-04-28
```

Create `docs/knowledge/graph/modules/frontend.apps.tw-app-staff.yaml`:

```yaml
schema_version: 1.0.0
id: frontend.apps.tw-app-staff
kind: module
name: 员工端 App
status: draft
summary: 承载员工端移动应用的前端代码与交互入口。
owners:
  - frontend
tags:
  - frontend
  - vue-ts
  - uniapp
module_type: frontend-app
stack: vue-ts
path: frontend/apps/tw.app.staff
source:
  declared_in: docs/knowledge/graph/modules/frontend.apps.tw-app-staff.yaml
  evidence:
    - frontend/apps/tw.app.staff/README.md
provenance:
  created_by: human
  created_at: 2026-04-28
  updated_by: human
  updated_at: 2026-04-28
```

Create `docs/knowledge/graph/modules/frontend.apps.tw-web-client.yaml`:

```yaml
schema_version: 1.0.0
id: frontend.apps.tw-web-client
kind: module
name: 客户 Web 端
status: draft
summary: 承载客户 Web 端应用的前端代码与交互入口。
owners:
  - frontend
tags:
  - frontend
  - vue-ts
module_type: frontend-app
stack: vue-ts
path: frontend/apps/tw.web.client
source:
  declared_in: docs/knowledge/graph/modules/frontend.apps.tw-web-client.yaml
  evidence:
    - frontend/apps/tw.web.client/README.md
provenance:
  created_by: human
  created_at: 2026-04-28
  updated_by: human
  updated_at: 2026-04-28
```

Create `docs/knowledge/graph/modules/frontend.apps.tw-web-ops.yaml`:

```yaml
schema_version: 1.0.0
id: frontend.apps.tw-web-ops
kind: module
name: 运营 Web 端
status: draft
summary: 承载运营 Web 端应用的前端代码与交互入口。
owners:
  - frontend
tags:
  - frontend
  - vue-ts
module_type: frontend-app
stack: vue-ts
path: frontend/apps/tw.web.ops
source:
  declared_in: docs/knowledge/graph/modules/frontend.apps.tw-web-ops.yaml
  evidence:
    - frontend/apps/tw.web.ops/README.md
provenance:
  created_by: human
  created_at: 2026-04-28
  updated_by: human
  updated_at: 2026-04-28
```

Create `docs/knowledge/graph/modules/frontend.apps.tw-web-portal.yaml`:

```yaml
schema_version: 1.0.0
id: frontend.apps.tw-web-portal
kind: module
name: 门户 Web 端
status: draft
summary: 承载门户 Web 端应用的前端代码与交互入口。
owners:
  - frontend
tags:
  - frontend
  - vue-ts
module_type: frontend-app
stack: vue-ts
path: frontend/apps/tw.web.portal
source:
  declared_in: docs/knowledge/graph/modules/frontend.apps.tw-web-portal.yaml
  evidence:
    - frontend/apps/tw.web.portal/README.md
provenance:
  created_by: human
  created_at: 2026-04-28
  updated_by: human
  updated_at: 2026-04-28
```

- [ ] **Step 5: Run generate, scan, tests, and commit**

Run:

```powershell
python tools/knowledge/knowledge.py generate
python tools/knowledge/knowledge.py check
python tools/knowledge/knowledge.py scan
python -m unittest tests.knowledge.test_knowledge_tool -v
```

Expected:

- `generate` exits `0`.
- `check` exits `0`.
- `scan` does not report any of the five `frontend/apps/*` paths as missing module nodes.
- Unit tests pass.

Commit:

```powershell
git add docs/knowledge/graph/contracts/.gitkeep docs/knowledge/proposals/.gitkeep docs/knowledge/graph/modules/frontend.apps.*.yaml docs/knowledge/generated tests/knowledge/test_knowledge_tool.py
git commit -m "feat: seed frontend knowledge modules"
```

### Task 7: Sync All Repository Skills Dynamically

**Files:**
- Modify: `tools/knowledge/knowledge.py`
- Modify: `tests/knowledge/test_knowledge_tool.py`

- [ ] **Step 1: Write failing dynamic sync tests**

Append:

```python
    def test_claude_skill_link_plan_scans_all_repository_skills(self):
        with isolated_repo() as root:
            for skill in ["tw-requirement-router", "brainstorming"]:
                path = root / ".agents" / "skills" / skill
                path.mkdir(parents=True)
                (path / "SKILL.md").write_text("---\nname: test\n---\n", encoding="utf-8")

            expected = knowledge.claude_skill_link_plan(root)

            self.assertEqual("../../.agents/skills/tw-requirement-router", expected["tw-requirement-router"])
            self.assertEqual("../../.agents/skills/brainstorming", expected["brainstorming"])
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_claude_skill_link_plan_scans_all_repository_skills -v
```

Expected: fails because `claude_skill_link_plan()` uses hardcoded `KNOWLEDGE_SKILLS`.

- [ ] **Step 3: Replace hardcoded skill list with directory scan**

Remove `KNOWLEDGE_SKILLS` and replace `claude_skill_link_plan()` with:

```python
def repository_skill_names(root: Path = REPO_ROOT) -> list[str]:
    source_root = root / ".agents" / "skills"
    if not source_root.exists():
        return []
    return sorted(
        path.name
        for path in source_root.iterdir()
        if path.is_dir() and (path / "SKILL.md").exists()
    )


def claude_skill_link_plan(root: Path = REPO_ROOT) -> dict[str, str]:
    return {
        skill: f"../../.agents/skills/{skill}"
        for skill in repository_skill_names(root)
    }
```

In `sync_claude_skills()`, keep `plan = claude_skill_link_plan(REPO_ROOT)` so runtime behavior now scans `.agents/skills/`.

- [ ] **Step 4: Run tests and commit**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool -v
python tools/knowledge/knowledge.py sync-skills --target claude
```

Expected:

- Unit tests pass.
- `sync-skills` creates or verifies relative symlinks for every skill directory under `.agents/skills/`.

Commit:

```powershell
git add tools/knowledge/knowledge.py tests/knowledge/test_knowledge_tool.py .claude/skills
git commit -m "feat: sync all repository skills"
```

### Task 8: Document Rules, Protocols, And Schema Authority

**Files:**
- Modify: `docs/knowledge/README.md`
- Modify: `docs/knowledge/memory.schema.json`
- Create: `tools/knowledge/rules/README.md`
- Create: `tools/knowledge/rules/knowledge-diagnostics.md`
- Modify: `tests/knowledge/test_knowledge_tool.py`

- [ ] **Step 1: Write failing documentation checks**

Append:

```python
    def test_schema_declares_python_validation_as_authority(self):
        schema = json.loads((knowledge.KNOWLEDGE_DIR / "memory.schema.json").read_text(encoding="utf-8"))

        self.assertIn("$comment", schema)
        self.assertIn("tools/knowledge/knowledge.py", schema["$comment"])

    def test_knowledge_readme_documents_new_stack_protocol(self):
        content = (knowledge.KNOWLEDGE_DIR / "README.md").read_text(encoding="utf-8")

        self.assertIn("新栈接入协议", content)
        self.assertIn("taxonomy.yaml 的变更必须先于新栈代码提交", content)
        self.assertIn("python tools/knowledge/knowledge.py scan", content)
        self.assertIn("python tools/knowledge/knowledge.py diff --from main --to HEAD", content)
        self.assertIn("python tools/knowledge/knowledge.py check-drift --from main --to HEAD --save", content)
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_schema_declares_python_validation_as_authority -v
python -m unittest tests.knowledge.test_knowledge_tool.KnowledgeToolTests.test_knowledge_readme_documents_new_stack_protocol -v
```

Expected: fails until README and schema are updated.

- [ ] **Step 3: Update schema authority note**

In `docs/knowledge/memory.schema.json`, add this top-level property directly after `$id`:

```json
  "$comment": "本文件仅作文档参考，实际校验由 tools/knowledge/knowledge.py 执行；两者不一致时以 Python 实现为准。",
```

Run:

```powershell
python -m json.tool docs/knowledge/memory.schema.json
```

Expected: exits `0`. Use `$comment` instead of JavaScript-style comments because the file must remain valid JSON.

- [ ] **Step 4: Update Knowledge README**

Append this section to `docs/knowledge/README.md`:

```markdown
## 新栈接入协议

taxonomy.yaml 的变更必须先于新栈代码提交。若代码先落地，`check-drift` 在首次 PR 时没有路径规则可匹配，首次提交会漏检。

标准步骤：

1. 在第一行代码之前约定目录结构，优先使用 `backend/{stack}/services/{name}/` 和 `backend/{stack}/packages/{name}/`。
2. 更新 `docs/knowledge/taxonomy.yaml` 的 `valid_stacks` 和 `path_rules`。
3. 运行全量扫描和图谱校验：

```powershell
python tools/knowledge/knowledge.py scan
python tools/knowledge/knowledge.py check
```

4. 单独提交 taxonomy 变更。
5. 新栈代码落地后运行：

```powershell
python tools/knowledge/knowledge.py scan
python tools/knowledge/knowledge.py init --kind module --id backend.go.services.example
python tools/knowledge/knowledge.py generate
python tools/knowledge/knowledge.py check
```

6. 后续 PR 使用增量兜底：

```powershell
python tools/knowledge/knowledge.py check-drift --from main --to HEAD
```

## 变更概览与历史记录

`diff` 只描述路径变更，不做漂移判断：

```powershell
python tools/knowledge/knowledge.py diff --from main --to HEAD
```

`check-drift --save` 会把诊断写入 `docs/knowledge/changes/<year>/`，文件格式与 `diagnostics.generated.json` 一致，并增加 `from_ref` 和 `to_ref`：

```powershell
python tools/knowledge/knowledge.py check-drift --from main --to HEAD --save
```

`docs/knowledge/proposals/` 只存放候选图谱更新，正式图谱仍需人工审核后写入 `docs/knowledge/graph/`。
```

- [ ] **Step 5: Create diagnostics rules docs**

Create `tools/knowledge/rules/README.md`:

```markdown
# Knowledge Diagnostics Rules

本目录记录 Knowledge Compiler 诊断规则。工具实现位于 `tools/knowledge/knowledge.py`，正式图谱位于 `docs/knowledge/graph/`，生成索引位于 `docs/knowledge/generated/`。

更新诊断行为时，同步更新 `knowledge-diagnostics.md`、`docs/knowledge/taxonomy.yaml` 和 `tests/knowledge/test_knowledge_tool.py`。
```

Create `tools/knowledge/rules/knowledge-diagnostics.md`:

```markdown
# Knowledge Diagnostics

| Code | Severity | Trigger | Related field |
|---|---|---|---|
| `knowledge.required-field` | error | 图谱节点缺少必填字段 | root required fields |
| `knowledge.schema-version` | error | `schema_version` 不符合 `x.y.z` | `schema_version` |
| `knowledge.id-format` | error | `id` 不是小写点分层级格式 | `id` |
| `knowledge.duplicate-id` | error | 多个图谱节点声明同一 id | `id` |
| `knowledge.invalid-kind` | error | `kind` 不在 `taxonomy.yaml valid_kinds` | `kind` |
| `knowledge.invalid-status` | error | `status` 不在 `taxonomy.yaml valid_statuses` | `status` |
| `knowledge.invalid-module-type` | error | `module_type` 不在 `taxonomy.yaml valid_module_types` | `module_type` |
| `knowledge.declared-in` | error | `source.declared_in` 缺失或不等于实际路径 | `source.declared_in` |
| `knowledge.source-format` | error | `source` 或 `source.evidence` 结构错误 | `source` |
| `knowledge.provenance-format` | error | `provenance` 结构错误 | `provenance` |
| `knowledge.missing-evidence` | warning | `source.evidence` 指向不存在的文件 | `source.evidence` |
| `knowledge.dangling-reference` | error | module/capability/decision/integration 引用不存在的图谱节点 | reference fields |
| `knowledge.missing-module` | error | taxonomy 命中的服务或应用路径没有 module 节点 | `path_rules` |
| `knowledge.missing-capability` | warning | taxonomy 命中的公共构件可能缺少 capability 或 module 节点 | `path_rules` |
| `knowledge.contract-drift` | error | 新增契约文件没有 contract 节点 | `path_rules` |
| `knowledge.contract-outdated` | warning | 已有 contract 节点对应的契约文件发生变更 | `path` |
| `knowledge.index-missing` | error | 生成索引缺失 | `generated` |
| `knowledge.index-invalid-json` | error | 生成索引不是合法 JSON | `generated` |
| `knowledge.index-stale` | error | 生成索引不是最新 | `generated` |
| `knowledge.index-obsolete` | error | 存在过期生成索引 | `generated` |
```

- [ ] **Step 6: Run documentation checks and commit**

Run:

```powershell
python -m json.tool docs/knowledge/memory.schema.json
python -m unittest tests.knowledge.test_knowledge_tool -v
python tools/knowledge/knowledge.py check
```

Expected: JSON parses, tests pass, and `check` exits `0`.

Commit:

```powershell
git add docs/knowledge/README.md docs/knowledge/memory.schema.json tools/knowledge/rules tests/knowledge/test_knowledge_tool.py
git commit -m "docs: document knowledge maintenance protocol"
```

### Task 9: Final Verification

**Files:**
- Modify only files from previous tasks if verification reveals a concrete defect.

- [ ] **Step 1: Run unit tests**

Run:

```powershell
python -m unittest tests.knowledge.test_knowledge_tool -v
python -m unittest tests.standards.test_standards_tool_v2 -v
```

Expected: all tests pass.

- [ ] **Step 2: Run Knowledge Compiler commands**

Run:

```powershell
python tools/knowledge/knowledge.py generate
python tools/knowledge/knowledge.py check
python tools/knowledge/knowledge.py scan
python tools/knowledge/knowledge.py diff --from HEAD~1 --to HEAD
python tools/knowledge/knowledge.py check-drift --from HEAD~1 --to HEAD --save
python tools/knowledge/knowledge.py query --text "前端" --limit 5
python tools/knowledge/knowledge.py query --text "Spring Boot 框架包" --limit 5
```

Expected:

- `generate` exits `0` and writes generated indexes.
- `check` exits `0`.
- `scan` no longer reports the five frontend app paths as missing modules.
- `diff` prints grouped Chinese output.
- `check-drift --save` writes a yearly JSON file under `docs/knowledge/changes/2026/`.
- `query --text "前端"` returns frontend app nodes.
- `query --text "Spring Boot 框架包"` exits `1` with no results until Java package nodes exist; this is acceptable because taxonomy can discover future Java package paths, but no formal node exists yet.

- [ ] **Step 3: Run standards check**

Run:

```powershell
python tools/standards/standards.py check
```

Expected: exits `0`.

- [ ] **Step 4: Inspect working tree**

Run:

```powershell
git status --short
git diff --stat
```

Expected: changes are limited to `docs/knowledge/**`, `tools/knowledge/**`, `tests/knowledge/**`, generated knowledge indexes, `.claude/skills/**`, and this feature's intentional commits. Existing unrelated `.gitignore` and `.claude/settings.local.json` changes remain untouched.

- [ ] **Step 5: Commit final generated artifacts**

Run:

```powershell
git add docs/knowledge tools/knowledge tests/knowledge .claude/skills
git commit -m "chore: finalize knowledge compiler plan-a adjustment"
```

Expected: commit succeeds if verification generated or fixed files. If `git status --short` shows no tracked changes from verification, no final commit is created.

## Self-Review Checklist

- Spec coverage:
  - Cross-stack directory conventions and taxonomy rules are covered by Task 1.
  - `check-drift` contract blind spot and `integration.contract` validation are covered by Task 2.
  - `scan` command is covered by Task 3.
  - `init` command and contract/integration/decision templates are covered by Task 4.
  - `diff` command and `changes/` persistence are covered by Task 5.
  - `graph/contracts/`, `proposals/`, and five frontend app seeds are covered by Task 6.
  - Dynamic skill sync is covered by Task 7.
  - schema authority, diagnostic docs, and new stack protocol are covered by Task 8.
  - Verification criteria are covered by Task 9.
- Placeholder scan:
  - The plan avoids deferred-work markers and placeholder code blocks.
- Type consistency:
  - New functions use `snake_case` and are introduced before command wiring uses them.
  - New diagnostics use the existing `Diagnostic`, `taxonomy_diagnostic()`, `error()`, and `warn()` conventions.
  - New graph directories follow existing plural directory names: `modules`, `capabilities`, `contracts`, `integrations`, `decisions`.
