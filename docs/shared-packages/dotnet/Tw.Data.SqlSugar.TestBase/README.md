# Tw.Data.SqlSugar.TestBase

`Tw.Data.SqlSugar.TestBase` provides database fixtures and reset helpers for tests only.

## Stability

The package is currently `experimental`. Promotion to `stable` requires container-backed tests that prove startup and disposal behavior, Respawn reset isolation, repeatability across test runs, and compatibility with each supported SqlSugar provider baseline.
