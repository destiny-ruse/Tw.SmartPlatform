# Agent 指令

本工程使用多智能体公共指令。

每轮对话开始时，凡涉及编码、评审、调试、重构、测试、构建、发布、文档、架构、配置或工程治理工作，Agent 必须先读取并遵守：

- `.rules\common-agent-instructions.md`

`AGENTS.md` 只作为 Agent 启动入口，不维护公共规则正文。公共规则变更必须修改 `.rules\common-agent-instructions.md`。
